version: '3.8'

# Strapi removed - using Shopify Pages instead
# Docker compose kept for future services if needed

services:
  # Add services here if needed in the future

networks:
  vampirevape-network:
    driver: bridge

